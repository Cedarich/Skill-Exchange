[package]
name = "skillxchange-certificates"
version = "0.1.0"
edition = "2021"
description = "NFT certificate minting contract for SkillXchange skill completions"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]
